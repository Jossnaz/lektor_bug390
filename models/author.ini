[model]
name = Author
label = {{ this.name }}
hidden = yes

[children]
replaced_with = site.get('/').pagination.items.filter(F.categories.contains(this))

[fields.name]
label = Name
type = string

[pagination]
enabled = yes
per_page = 10
